#head
  #head_cen
    .head_height
      .head-serach        
        .pull-left
          =link_to (image_tag "en.png", :class => "language-icon"), change_language_path(:language => "en")
          =link_to (image_tag "fa.png", :class => "language-icon"), change_language_path(:language => "fa")
        .clrear-float
      #head-menu
        %h1.logo
          %a{href: "index.html"} M CUBE
        %ul
          %li
            = link_to t("home"), homedesign_path, :class => "active"           
          %li
            = link_to (t"services_section.title"), Service
          %li
            = link_to (t"products_section.title"), Product       
          -Page.all.each do |page|  
            %li
              = link_to page.title, page_path(page)
