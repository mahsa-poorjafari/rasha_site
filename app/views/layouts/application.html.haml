!!!
%html
  %head
    %meta{:charset => "utf-8"}
    -set_meta_tags :keywords => %w[Rasha WebSite keywords "راشا"]    
    -set_meta_tags :description => "خانه هوشمند, شرکت راشا, Smart Home"
    -set_meta_tags :site => 'Site Title', :title => 'Rasha WebSite!', :reverse => true
    = yield(:refresh_meta) if content_for?(:refresh_meta)
    %title
      ایده پردازان هوشمند راشا  #{@head.fetch(:title, '') if @head}
    =stylesheet_link_tag "application", :media => "all"
    =favicon_link_tag 'favicon.png', rel:'shortcut icon', type: 'image/png'
    =javascript_include_tag "application"
    =csrf_meta_tags
  %body
    .total-division
      = render "shared/menu_bar"   
      .div-slider
        = render 'slider'
      #content
        =yield      
      = render 'shared/footer'
    
