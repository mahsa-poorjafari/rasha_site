!!!
%html{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}
    %meta{:name => "keywords", :content => @keywords || "industrial website keywords"}
    %meta{:name => "description", :content => @page_desc || "industrial website!"}
    = yield(:refresh_meta) if content_for?(:refresh_meta)
    %title
      industrial web site | #{@head.fetch(:title, '') if @head}
    =stylesheet_link_tag "application", :media => "all"
    =javascript_include_tag "application"
    =csrf_meta_tags
  %body
    =render "shared/menu_bar"
    .container
      =yield
      %hr/
      %footer
        %p © راش ۱۳۹۲
