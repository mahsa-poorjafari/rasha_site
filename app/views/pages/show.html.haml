.list-div
  #welcom_pan
    %h2.heading2
      %span 
        = @page.title
         
    .show-des-en
      =raw @page.page_html
    -if @page.read_attribute("title") == "تماس با ما"
      = render "shared/msg_form"
    -if is_admin?
      #quotPan 
        .footer_Link
          = link_to content_tag("span", "ویرایش"), edit_page_path(@page)
          \|
          = link_to content_tag("span", "بازگشت"), home_path
  

